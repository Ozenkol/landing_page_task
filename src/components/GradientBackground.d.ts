import React from "react";
interface GradientBackgroundProps {
    children: React.ReactNode;
    imageUrl?: string;
    className?: string;
}
export default function GradientBackground({ children, imageUrl, className, }: GradientBackgroundProps): import("react/jsx-runtime").JSX.Element;
export {};
